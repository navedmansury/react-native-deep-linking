import React from 'react';

import {Text, View} from 'react-native';

const Edit = ({navigation, route}) => {
  return (
    <View>
      <Text>Edit Js</Text>
    </View>
  );
};

export default Edit;
